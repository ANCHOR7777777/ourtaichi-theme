{%- liquid
  assign threshold = settings.discount_threshold | times: 100
  assign remaining_amount = threshold | minus: cart.total_price
-%}

{%- if cart.total_price < threshold and threshold > 0 -%}
  <div class="cart-discount-banner">
    <p>🎉 {{ settings.discount_message | replace: '{{amount }}', remaining_amount | money }}</p>
  </div>
{%- endif -%}

<style>
  .cart-discount-banner {
    text-align: center;
    padding: 0.5rem;
    margin-bottom: 1rem;
    background-color: #f8f8f8;
    border-radius: 4px;
  }
</style>
